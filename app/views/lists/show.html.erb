<% title "List" %>

<p>
  <strong>Name:</strong>
  <%=h @list.name %>
  <br />
  <ul>
    <% for inv in @list.invitations %>
    <li>
        <%= h inv.guest.username %>
    </li>
    <% end %>
  </ul>
</p>

<p>
  <%= link_to "Edit", edit_list_path(@list) %> |
  <%= link_to "Destroy", @list, :confirm => 'Are you sure?', :method => :delete %> |
  <%= link_to "View All", lists_path %>
</p>
